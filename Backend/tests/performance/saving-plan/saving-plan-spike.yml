config:
  target: "http://localhost:3000"
  phases:
    - duration: 10
      arrivalRate: 1
      name: baseline
    - duration: 30
      arrivalRate: 20
      name: spike
    - duration: 10
      arrivalRate: 1
      name: cooldown
  defaults:
    headers:
      Authorization: "Bearer test-token"
      Content-Type: "application/json"

scenarios:
  - name: saving_plan_spike_test
    flow:
      - get:
          url: "/saving-plan"
      - post:
          url: "/saving-plan"
          json:
            planType: "Advanced"
            householdSize: 3
            priorityArea: "Kitchen"
            waterSource: "Well"
          capture:
            - json: "$._id"
              as: "planId"
      - get:
          url: "/saving-plan/calculation"
      - put:
          url: "/saving-plan/{{ planId }}"
          json:
            status: "Inactive"